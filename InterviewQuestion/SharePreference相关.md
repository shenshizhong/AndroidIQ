

SharePreference 注意点？
```
SharePreference 是线程安全的，但进程不安全。
commit 是同步写入，有返回值。apply 是异步写入，无返回值。

commit 是保存到磁盘中的，在多个并发的情况下，他们会等待，减低了效率。
而apply 是异步写到内存中的，后面有新提交的立马覆盖之前的数据，所以速度更快。

当你关心提交结果，而且还有后续的操作的时候，就要用commit
```